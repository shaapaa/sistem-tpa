import { RoleLayout } from "@/components/layout/role-layout";

export default function PengajarLayout({ children }: { children: React.ReactNode }) {
  return <RoleLayout role="PENGAJAR">{children}</RoleLayout>;
}