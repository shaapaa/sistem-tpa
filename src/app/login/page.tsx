"use client"

import { useState } from "react"
import { useRouter } from "next/navigation"
import { createClient } from "@/lib/supabase/client"
import { Button } from "@/components/ui/button"
import { Input } from "@/components/ui/input"
import { Label } from "@/components/ui/label"
import { Eye, EyeOff, ArrowUpRight, Sparkles } from "lucide-react"

export default function LoginPage() { const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [showPassword, setShowPassword] = useState(false); const [error, setError] = useState<string | null>(null); const [loading, setLoading] = useState(false); const router = useRouter(); const supabase = createClient();
  const handleLogin = async (e: React.FormEvent) => { e.preventDefault(); setLoading(true); setError(null); const { error } = await supabase.auth.signInWithPassword({ email, password }); if (error) { setError(error.message); setLoading(false); return } const { data: { user } } = await supabase.auth.getUser(); if (user) { const { data: profile } = await supabase.from("users").select("role").eq("id", user.id).single(); router.push(profile?.role === "ADMIN" ? "/admin" : profile?.role === "PENGAJAR" ? "/pengajar" : "/orang-tua") } }
  return <main className="warm-grid flex min-h-screen items-center justify-center p-4 sm:p-8"><div className="grid w-full max-w-5xl overflow-hidden rounded-[2rem] border border-border/70 bg-card shadow-2xl shadow-primary/10 lg:grid-cols-[1.05fr_.95fr]"><section className="relative hidden min-h-[600px] flex-col justify-between overflow-hidden bg-primary p-10 text-primary-foreground lg:flex"><div className="absolute -right-20 -top-20 size-72 rounded-full border border-primary-foreground/15" /><div className="absolute -right-8 -top-8 size-48 rounded-full border border-primary-foreground/15" /><div className="relative"><div className="flex items-center gap-3"><div className="flex size-11 items-center justify-center rounded-2xl bg-accent text-accent-foreground"><Sparkles /></div><div><p className="font-bold">Baitul Yatama</p><p className="text-xs text-primary-foreground/60">TPA Monitoring</p></div></div></div><div className="relative max-w-md"><p className="mb-5 text-sm font-semibold uppercase tracking-widest text-primary-foreground/55">Ruang tumbuh bersama</p><h1 className="text-5xl font-bold leading-[1.05] tracking-tight text-balance">Mendampingi langkah kecil, setiap hari.</h1><p className="mt-6 max-w-sm text-sm leading-6 text-primary-foreground/70">Kelola kehadiran, jadwal, dan perkembangan santri dalam satu ruang yang sederhana dan bermakna.</p></div><div className="relative flex items-center justify-between border-t border-primary-foreground/15 pt-5 text-xs text-primary-foreground/55"><span>TPA Baitul Yatama</span><span className="flex items-center gap-1">Masuk ke ruang Anda <ArrowUpRight /></span></div></section><section className="flex min-h-[600px] flex-col justify-center p-6 sm:p-12"><div className="mb-10 lg:hidden"><div className="mb-5 flex size-12 items-center justify-center rounded-2xl bg-primary text-primary-foreground"><Sparkles /></div><p className="text-sm font-bold">Baitul Yatama</p><p className="text-xs text-muted-foreground">TPA Monitoring</p></div><div className="mx-auto w-full max-w-sm"><p className="eyebrow">Selamat datang kembali</p><h2 className="mt-3 text-3xl font-bold tracking-tight text-balance">Masuk ke akun Anda</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">Lanjutkan mendampingi perkembangan santri hari ini.</p><form onSubmit={handleLogin} className="mt-8 flex flex-col gap-5"><div className="flex flex-col gap-2"><Label htmlFor="email">Email</Label><Input id="email" type="email" placeholder="email@contoh.com" value={email} onChange={(e) => setEmail(e.target.value)} required autoComplete="email" className="h-11" /></div><div className="flex flex-col gap-2"><Label htmlFor="password">Password</Label><div className="relative"><Input id="password" type={showPassword ? "text" : "password"} placeholder="Masukkan password" value={password} onChange={(e) => setPassword(e.target.value)} required className="h-11 pr-11" autoComplete="current-password" /><button type="button" aria-label={showPassword ? "Sembunyikan password" : "Tampilkan password"} onClick={() => setShowPassword(!showPassword)} className="absolute right-3 top-1/2 -translate-y-1/2 text-muted-foreground hover:text-foreground">{showPassword ? <EyeOff /> : <Eye />}</button></div></div>{error && <p className="animate-slide-up rounded-lg bg-destructive/10 px-3 py-2 text-sm text-destructive" role="alert">{error}</p>}<Button type="submit" className="h-11 w-full" disabled={loading}>{loading ? "Memeriksa akun..." : "Masuk ke dashboard"}</Button></form></div></section></div></main> }
